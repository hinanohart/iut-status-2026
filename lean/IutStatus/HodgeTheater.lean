/-
IutStatus.HodgeTheater — Θ±ellNF-Hodge theater structure stub.

Drift-zero: maps to `iut:HodgeTheater` in `data/entities.json`.
The principal scaffolding of IUTchI Section 4 — the carrier of the
theta-link and log-link.
-/

import IutStatus.Frobenioid

namespace IutStatus

/-- Θ±ellNF-Hodge theater. Stub. -/
structure HodgeTheater : Type where
  /-- placeholder field to be replaced by LANA. -/
  placeholder : Unit := ()

end IutStatus
