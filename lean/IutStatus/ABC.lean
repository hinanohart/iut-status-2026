/-
IutStatus.ABC — abc conjecture and its IUT-route specialisation stub.

Drift-zero: maps to `iut:abc_conjecture` and
`iut:abc_special_case_via_iut` in `data/entities.json`.
-/

import IutStatus.HeightInequality
import IutStatus.Diophantine

namespace IutStatus

/-- abc conjecture (Masser-Oesterlé 1985). Stub. -/
theorem abc_conjecture : Prop := by sorry

/-- abc conjecture special case via IUT (IUTchIV Cor 2.2 or related). Stub. -/
theorem abc_special_case_via_iut : Prop := by sorry

end IutStatus
