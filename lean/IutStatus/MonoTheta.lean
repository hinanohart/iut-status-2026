/-
IutStatus.MonoTheta — mono-theta environment stub.

Drift-zero: maps to `iut:mono_theta_environment` in `data/entities.json`.
The data-side record carries no `lean_stub`; this stub is a minimal
placeholder per the v0.7.5 split (every `lean_module` reference must
resolve to a file).
-/

import IutStatus.EtaleTheta

namespace IutStatus

/-- Mono-theta environment placeholder axiom. Stub. -/
axiom mono_theta_environment : Prop

end IutStatus
